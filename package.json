{
  "name": "babelbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["core"],
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "npm test -w core",
    "typecheck": "npm run typecheck -w core"
  }
}
